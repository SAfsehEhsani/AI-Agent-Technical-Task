# pytest.ini
[pytest]
# Add the root directory to the Python path for module discovery
pythonpath = .
# Optional: Specify test discovery locations (usually not needed with default pytest)
# testpaths = part1/tests